[package]
name = "legacy-ios-test-support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
legacy-ios-core = { path = "../core" }
thiserror.workspace = true
tokio = { workspace = true, features = ["io-util"] }

[lints]
workspace = true
