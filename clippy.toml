disallowed-methods = [
  { path = "std::process::Command::new", reason = "host subprocesses are outside the architecture" },
  { path = "tokio::process::Command::new", reason = "host subprocesses are outside the architecture", allow-invalid = true },
]
